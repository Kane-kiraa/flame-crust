# Flame & Crust Frontend

React + JavaScript + Vite + Tailwind CSS frontend.

```bash
npm install
npm run dev
```

The app runs on `http://localhost:3000` and reads menu data from the Spring Boot API at `http://localhost:8080/api`.

The API URL can be changed with `VITE_API_URL` in `.env`.
